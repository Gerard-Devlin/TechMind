# Ignore everything at the repo root by default
/*

# Keep this file so the ignore rules stay versioned
!.gitignore

# Allow the my-project folder itself
!my-project/

# Re-ignore everything inside that folder
my-project/*

# Only track the docs directory (and its contents)
!my-project/docs/
!my-project/docs/**
