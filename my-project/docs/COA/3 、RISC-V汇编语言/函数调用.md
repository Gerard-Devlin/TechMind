[TOC]

---

## 一、栈

函数调用:

1. **参数传递** → 放进 `a0–a7`
2. **跳转** → `jal`
3. **分配空间** → 用 stack（`sp`）
4. **执行函数**
5. **返回值** → 放进 `a0`
6. **返回** → `jr ra`

!!! danger

    会出现问题，如果我嵌套调用函数，那么我的 `ra` 会被重复赋值，也就是跳转回去的索引丢失了。索引丢失也就是返回地址无法找回。

结论：在调用下一个函数之前，把 `ra` 存到栈中。因为栈在内存中空间很大可以存很多很多地址，不像寄存器非常有限。 

### 1、一般函数

```cpp
int f = (g + h) - (i + j);
```

```asm
# 函数：f = (g + h) - (i + j)
# 参数：
#   a0 = g
#   a1 = h
#   a2 = i
#   a3 = j
# 返回：
#   a0 = f

Leaf:
    # -----------------------------
    # ① 开栈（分配 8 字节）
    # 因为要用 s0、s1（每个 4 字节）
    # -----------------------------
    addi sp, sp, -8

    # -----------------------------
    # ② 保存 s 寄存器
    # s寄存器属于 callee-saved
    # 只要用了，就必须恢复
    # -----------------------------
    sw s1, 4(sp)
    sw s0, 0(sp)

    # -----------------------------
    # ③ 计算 (g + h)
    # s0 = a0 + a1
    # -----------------------------
    add s0, a0, a1

    # -----------------------------
    # ④ 计算 (i + j)
    # s1 = a2 + a3
    # -----------------------------
    add s1, a2, a3

    # -----------------------------
    # ⑤ 做减法
    # a0 = (g+h) - (i+j)
    # 返回值必须放在 a0
    # -----------------------------
    sub a0, s0, s1

    # -----------------------------
    # ⑥ 恢复 s寄存器（非常重要）
    # 把调用前的值还回去
    # -----------------------------
    lw s0, 0(sp)
    lw s1, 4(sp)

    # -----------------------------
    # ⑦ 关栈（释放空间）
    # -----------------------------
    addi sp, sp, 8

    # -----------------------------
    # ⑧ 返回调用者
    # -----------------------------
    jr ra
```

```
高地址
│
│   s1  ← sp + 4
│   s0  ← sp + 0
↓
sp（当前）
```

- 中间结果用 s0 / s1
- 最终结果放 a0

### 2、嵌套函数

```cpp
int sumSquare(int x, int y) {
    return mult(x,x) + y;
}
```

```asm
sumSquare:
    # -----------------------------
    # ① 开栈（分配 8 字节空间）
    # 用来存：
    #   - ra（返回地址）
    #   - y（a1）
    # -----------------------------
    addi sp, sp, -8

    # -----------------------------
    # ② 保存 ra（回 main 的地址）
    # 因为后面 jal mult 会覆盖 ra
    # -----------------------------
    sw ra, 4(sp)

    # -----------------------------
    # ③ 保存 y（a1）
    # 因为后面要改 a1 = x
    # -----------------------------
    sw a1, 0(sp)

    # -----------------------------
    # ④ 准备调用 mult(x, x)
    # 当前：
    #   a0 = x
    #   a1 = y
    # 变成：
    #   a0 = x
    #   a1 = x
    # -----------------------------
    mv a1, a0

    # -----------------------------
    # ⑤ 调用 mult
    # jal 做两件事：
    #   1. ra = 返回到下一行
    #   2. PC 跳到 mult
    # -----------------------------
    jal mult

    # -----------------------------
    # ⑥ mult 返回后：
    #   a0 = mult(x, x)
    #   a1 已经不可靠（被改过）
    # 所以恢复 y
    # -----------------------------
    lw a1, 0(sp)

    # -----------------------------
    # ⑦ 做最终计算：
    #   a0 = mult(x,x) + y
    # 返回值必须放在 a0
    # -----------------------------
    add a0, a0, a1

    # -----------------------------
    # ⑧ 恢复 ra（回 main 的地址）
    # 如果不恢复，后面 jr ra 会跳错地方
    # -----------------------------
    lw ra, 4(sp)

    # -----------------------------
    # ⑨ 关栈（释放 8 字节）
    # -----------------------------
    addi sp, sp, 8

    # -----------------------------
    # ⑩ 返回调用者（main）
    # PC = ra
    # -----------------------------
    jr ra
```

- 嵌套调用 → **必须存 `ra`**

---

## 二、寄存器约定

### 1、Caller-saved（调用者负责）

这些寄存器**函数调用后可能会变**

规则是如果 caller 还要用，**必须自己先存**

```
a0–a7   参数 / 返回值
t0–t6   临时变量
ra      返回地址
```

### 2、Callee-saved（被调用者负责）

这些寄存器**函数调用后必须保持不变**

规则是callee 如果用了，**必须恢复**

```
s0–s11
sp（栈指针）
```

| 类型   | 寄存器        | 用途        | 谁负责 |
| ------ | ------------- | ----------- | ------ |
| zero   | x0            | 永远是0     | -      |
| ra     | x1            | 返回地址    | caller |
| sp     | x2            | 栈指针      | callee |
| a0–a7  | x10–x17       | 参数/返回值 | caller |
| t0–t6  | x5–x7,x28–x31 | 临时变量    | caller |
| s0–s11 | x8,x9,x18–x27 | 保存寄存器  | callee |

!!! success "判断寄存器保存规则"

    - 先看有没有 jal：
    如果有 jal → 当前函数是 caller → 必须考虑保存 ra，以及后面还要用到的 a / t 寄存器（否则会被覆盖）

    - 再看有没有使用 s 寄存器：
    如果用了 s → 当前函数是 callee → 必须在使用前保存 s，用完之后恢复（保证调用前后不变）判断寄存器保存规则：

